<%= form_with(model: promise, local: true) do |form| %>
  <% if promise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(promise.errors.count, "error") %> prohibited this promise from being saved:</h2>

      <ul>
      <% promise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :contributions %>
    <%= form.text_field :contributions, id: :promise_contribution %>
  </div>

  <div class="field">
    <%= form.label :donation %>
    <%= form.text_field :donation, id: :promise_donation %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
