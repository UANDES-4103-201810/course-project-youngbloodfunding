<%= form_with(model: package, local: true) do |form| %>
  <% if package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(package.errors.count, "error") %> prohibited this package from being saved:</h2>

      <ul>
      <% package.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :bundle_name %>
    <%= form.text_field :bundle_name, id: :package_bundle_name %>
  </div>

  <div class="field">
    <%= form.label :bundle_description %>
    <%= form.text_field :bundle_description, id: :package_bundle_description %>
  </div>

  <div class="field">
    <%= form.label :bundle_price %>
    <%= form.text_field :bundle_price, id: :package_bundle_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
