<%= form_with(model: created_project, local: true) do |form| %>
  <% if created_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(created_project.errors.count, "error") %> prohibited this created_project from being saved:</h2>

      <ul>
      <% created_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
