<% if user_signed_in? && current_user.role == 1 %>
  <div class="well">
    <h2><%= current_user.name %> <%= current_user.last_name %></h2>

      <ul class="nav nav-pills">
        <li role="presentation" class="btn btn-primary">
          <div><%= link_to 'Users', users_index_path, {:style => "color:#fff;"}%></div>
        </li>
        <li role="presentation" class="btn btn-primary" style="margin-left: 10px">
          <div><%= link_to 'Categories', categories_path, {:style => "color:#fff;"}%></div>
        </li>
        <li role="presentation" class="btn btn-primary" style="margin-left: 10px">
          <div><%= link_to 'Projects', projects_path, {:style => "color:#fff;"}%></div>
        </li>
      </ul>
  </div>
<% end %>